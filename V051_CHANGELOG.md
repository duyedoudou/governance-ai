# V0.5.1 Changelog — 对话意图路由

## 目标

让“AI村长”先判断用户是在闲聊、询问系统使用方法，还是在做治理工作；不再把所有无法识别的问题默认落到人口台账。

## 新增

- 新增 Netlify Edge Function：`netlify/edge-functions/intent-router.ts`。
- 在 `/api/query` 和 `/api/reference/document/query` 前增加意图路由层。
- 支持 5 种模式：
  - `chat`：闲聊，不查询数据库。
  - `system_help`：系统使用帮助，不查询数据库。
  - `governance_query`：治理数据/政策查询，继续进入原有受控数据库查询链路。
  - `document_query`：当前锁定参考文档时继续查询该文档。
  - `clarify`：意图不清时先追问，不执行数据库查询。
- 新增轻量闲聊 UI：`site/intent-ui.js`。
- 闲聊回复显示“闲聊 · 未查询数据库”，不再伪装成数据核验结果。
- 当前锁定 DOCX 时，“谢谢”“辛苦了”“来聊天”等仍可即时切换为闲聊；后续再问文档内容时仍沿用文档上下文。
- 当前锁定 DOCX 时，如果问题明确转向村级治理数据，路由器会退出文档查询链路并交给治理查询。

## 核心安全规则

1. 无法识别治理意图，不等于人口查询。
2. 不确定时优先 `clarify`，不碰数据库。
3. 闲聊和系统帮助不调用治理数据库。
4. 治理事实仍由原有确定性数据库 Skill 计算，模型只负责意图判断和语言表达。
5. 当前后端不能可靠执行的治理问题，不允许为了“给出答案”而默认返回全村人口。

## 典型行为

- `来聊天` → 闲聊，不查数据库。
- `谢谢` → 闲聊，不查数据库。
- `村里最近挺热闹啊` → 更倾向闲聊，不因出现“村里”而查人口。
- `查一下70岁以上老人` → 治理查询。
- `看2026年养老保险未缴名单` → 治理查询。
- `怎么上传资料` → 系统使用帮助。
- `查一下` → 意图确认，不默认返回160人。
- DOCX 上下文中 `人员配置呢？` → 继续文档查询。
- DOCX 上下文中 `谢谢` → 闲聊。

## 数据库

V0.5.1 不新增、不修改数据库 migration。
