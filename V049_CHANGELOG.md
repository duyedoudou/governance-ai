# V0.4.9 变更记录

## 1. 管理员入口
- 桌面端右上角由难以识别的圆形“演”改为明确的“演示管理员⌄”。
- 数据治理仍位于管理员菜单内，不提升为普通一级导航。
- 移动端统一显示“管理员 · 数据治理”。

## 2. 参考资料连续追问
- 从参考数据点击“基于此资料问 AI”后，真实 `asset_id` 会成为会话级参考上下文。
- 第一问完成后不再清空上下文。
- 后续“其中女性呢 / 再按村组分 / 导出来”等追问继续使用同一 `asset_id`。
- 输入框上方明确展示“当前参考资料”，并提供“退出当前资料”。

## 3. 上传资料 dataset / version
- 新增 `reference_datasets` 数据集表。
- `asset_id` 表示一次具体上传；`dataset_id` 表示长期业务数据集。
- 管理员发布时确认“数据集名称”。同分类同数据集名称再次发布时自动生成 v2/v3…。
- 同一数据集只保留一个 `is_current=true` 的当前版本；参考数据列表和模型动态目录默认只暴露当前版本。
- 历史 asset 仍保留，可用于审计和证据追溯。
- 通用查询同时支持真实 `asset_id` 和 `dataset_id`；dataset_id 会解析到当前已发布 asset。
