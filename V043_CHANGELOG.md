# V0.4.3 变更说明

## 用户反馈 1：回答 UI 不清晰

处理：
- 结论优先，大数字/KPI 在第一视觉层级。
- 明细类结果直接显示表格。
- “结果说明”降级为可展开项。
- 数据依据常驻底部；Query Plan/Skill Trace 改为点击查看。

## 用户反馈 2：模型遗漏名单

处理：
- 新增 `validatePlan()`。
- 原问题包含名单语义时强制 `detail=true`。
- 这层校验在 GPT 计划和规则兜底计划之后统一执行。

## 用户反馈 3：左侧入口没有详情

处理：
- 数据目录：详情抽屉 + 字段 + 来源 + 规模 + 可回答问题。
- 政策文件：详情抽屉 + 条款 + 适用对象 + 来源。
- 业务台账：新增 `/api/ledgers`，支持多个台账卡片、统计摘要和脱敏预览。
- 所有详情都支持“基于这份数据提问”。
