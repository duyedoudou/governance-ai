[build]
  publish = "site"
  command = "echo V0.5.3.2 governance agent + frontend contract fix + Agent Planner + Hybrid Search"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
