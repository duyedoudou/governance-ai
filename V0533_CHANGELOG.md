# V0.5.3.3 修复说明

本版只处理已确认的基础回归，不扩展新的业务能力。

- 修复 AI村长头像：替换被截断的 PNG，并增加独立备用头像文件。
- 强制头像缓存刷新到 `v=0533`，主头像加载失败时切换到不同文件名的备用头像。
- “介绍下 / 介绍一下 / 自我介绍 / 说说你自己”等直接进入自我介绍，不再误送 Agent。
- Agent 改为先规划、后按需初始化数据库；普通聊天/一般讨论不再依赖数据库初始化。
- 结构化查询继续委托 V0.5.2 QuerySpec；知识检索继续保留 V0.5.3 Hybrid Search。
- Agent 下游 500 继续被用户界面兜底，但执行计划保留下游状态码用于诊断。
