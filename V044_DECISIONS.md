# V0.4.4 产品决策整理

本版本把最近几轮讨论统一落实为以下产品原则。

## 1. AI 是主入口
- 一级导航只突出：智能问答、历史记录、收藏/常用。
- 数据目录、政策文件、业务台账不再作为一级入口。
- 原始资料统一降级到「资料与依据 → 参考数据」。
- 用户默认通过自然语言完成工作，需要人工核验时再逐层进入底层资料。

## 2. 输入框永远可触达
- 智能问答输入框固定在主内容区底部。
- 中间回答区独立滚动，页面滚动不会带走输入框。
- 聊天入口不提供「添加文件」。

## 3. 普通 Agent 只读
普通用户侧允许：查询、筛选、统计、关联、政策解释、查看依据、查看原始数据、导出结果。
普通用户侧不允许：上传、增加、修改、删除、回写正式数据。

## 4. 数据治理是独立管理员入口
管理员从头像菜单进入「数据治理」。
流程：
1. 在外部先清洗数据。
2. 上传已清洗文件。
3. 系统解析结构与样例。
4. GPT 自动建议分类；失败时规则兜底。
5. 管理员确认/修改分类。
6. 点击发布。
7. 只有发布后 AI 才能读取。

「上传成功」与「发布给 AI」严格分离。

## 5. 自动分类会生长参考数据文件夹
- 发布到已有分类：自动进入原分类文件夹。
- 管理员输入一个新的分类名：发布时自动创建分类文件夹。
- 参考数据的分类不是前端写死，而是从数据库动态读取。

## 6. 源文件与 AI 查询层分开
- 源文件：保存到 Netlify Blobs，用于人工核验和追溯。
- Excel/CSV/JSON：解析成结构化 JSON 记录，保存到 Postgres，供 AI 的受控 Skill 查询。
- TXT/MD：保存原文件，同时保存可检索文本。
- PDF/Word：当前版本保存和分类原文件，但不假装已经完成全文解析；因此不会进入 AI 的可查询动态目录，直到后续加入可靠的全文解析流程。

## 7. 原始数据可以找到，但不是第一入口
查原始数据的路径刻意设计为三级：
AI回答 → 查看依据 → 资料与依据 / 参考数据 → 分类文件夹 → 数据集/文件 → 原始记录或源文件。
工作人员始终可以人工复核，但产品心智仍然是「先问 AI」。

## 8. 动态数据集进入 AI
新增受控 Skill：`reference_dataset_query`。
它只能查询「数据治理端已发布、且可被 AI 读取」的数据资产；不会读取待确认上传，也不会读取尚未解析的二进制文档。
